class FavRecipesController < ApplicationController

  def index
    # @user = User.find(params[:userId])
    # recipes = []; 
    # @user.fav_recipes.each do |i|
    #   if i.recipe[:yummily_id != nil] 
    #     recipes << {
    #       name: i.recipe[:name],
    #       preparation: i.recipe[:preparation]
    #     }
    #   end
    # end
    # json_response(recipes)
  end

  def create
  end

  def destroy
  end

end